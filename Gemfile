source "https://rubygems.org"
gem 'jekyll'

group :jekyll_plugins do
# NOTE: Jekyll-feed already included with GitHub pages, so should be abble to skip
    gem 'jekyll-feed'

    gem 'jekyll-paginate'
#   gem 'jekyll-seo-tag'
  end